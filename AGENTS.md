# oya-inai-keikaku-soudan — 親なき後支援の知識グラフ運用マニュアル

このファイルは本 Vault で作業する AI エージェントへの操作指示書の**正典**です（`CLAUDE.md`・`GEMINI.md` は本ファイルを指す入口）。「なぜ存在するか」は `purpose.md`、「何をどう書くか」は `schema.md` が定め、本ファイルは「どう動くか」だけを定めます。

> **この Vault の一人称は「相談支援専門員」です。** 計画相談を担う者と具体的支援を行う者は別法人に属し、本人に関する情報は親からの聴き取り・支援者会議・各事業所からの提供・計画とモニタリングという形で、すべて**相談支援専門員という結節点**を経由して集まります。本 Vault はその結節点に集まる情報を、本人単位の暗黙知として編むための装置です。

> **重要**: すべての操作前に `purpose.md` を読んでください。特に §3-0 の根本原則——本人がいかなる状態にあっても、本人に関することは本人に決める権利がある（第一の柱）。本人と関わるすべての人にも同じ権利がある（第二の柱）。二つは一つの原則の二側面であり、本 Vault のすべての記述・運用・判断はこの双方向性を起点とします。全文は purpose.md §3・§5。

---

## §0 AI 要件 — この Vault を担う AI が満たすべき能力の契約

ingest を担う AI エージェントは、以下をすべて満たすこと。満たせない環境では wiki/ への書き込みを行わず（読み取り・下書き提案までに留める）、その旨を使い手に伝える。

1. **Vault 内ファイルの読み書き**ができること（Markdown の読解・作成・部分編集）
2. **日本語**での読解・生成ができること
3. **本指示書と黙認方式**（§2-1）を遵守できること
4. **`python3 scripts/okf_lint.py` を実行**でき、終了コード `2`（ERROR）で作業を停止できること。Python3 が使えない環境では、wiki/ への書き込みを行わないこと
5. **承認ゲート**を尊重できること — wiki/ への書き込み前に必ず使い手の明示的承認を得る（§2-2）

---

## §1 Vault 構造

| 場所 | 役割 |
|------|------|
| `purpose.md` / `schema.md` / `schema-common.md` | 魂 ／ 構造ルール ／ 姉妹 Vault と共通の構造ルール |
| `index.md` / `log.md` / `overview.md` | カタログ（ingest 毎に更新）／ 操作ログ（append-only）／ 俯瞰 |
| `docs/` | watchlist・導入手順・マニュアル・支援の輪の確認事項・review-system / graph-insight / 連携 |
| `raw/` | 生ソース。**追記専用**（§2-1）。提供主体別の8棚: `10_本人・家族から/` `20_会議/` `30_事業所から/` `40_後見・法律から/` `50_医療から/` `60_行政・制度/` `70_自分の作成物/` `90_assets/` |
| `wiki/` | AI 生成ページ（承認後に書き込み）。`persons/` を中心に15型。型と配置の対応は schema.md §5 |
| `templates/` / `記入例/` | 雛形 ／ 架空の利用者による記入例（研修・画面共有はこちら） |
| `scripts/` / `githooks/` | lint と pre-commit の関所（scripts/README.md） |

### 受付箱（Vault の外）

Vault の**外**に受付箱を1つ置く。既定は `~/Desktop/受付箱`（別の場所に置く場合はデスクトップにショートカットを1つ）。

- 使い手は、聞き取り資料・提供資料・様式の写しを**受付箱に置くだけ**。ここが原本の唯一の入口である
- AI は受付箱を読み、§3-1 Step 0 の宣言に従って**原本を `raw/` の該当する棚へ移動**する。コピーではなく移動なので、受付箱は処理が済めば空になる
- **受付箱を `raw/` の下や Vault の中に作らない。**`raw/` は追記専用（§2-1）で、入った時点で取り消せない。受付箱を外に置くことで、置き間違いを記録前に取り消せる境界が生まれる
- **削除という操作を運用に含めない。**受付箱は「中身が棚へ移ることで空になる」
- AI に見せるフォルダは **Vault ／ スキルの置き場所 ／ 受付箱** の3つ（docs/導入手順.md Step 1）

---

## §2 絶対遵守のガードレール

1. **`raw/`は追記専用（append-only）。** 既存ファイルの**内容の書き換え・削除は禁止**（生ソースを失うと取り返せない）。新規ファイルの受け入れ保存と、棚の間違いの**振り分け直し（内容無変更の移動）**は、AI が振り分け先を**宣言**し（質問しない）、訂正がなければそのまま実行してよい（黙認方式）。迷っても「未分類」を作らず最有力の棚に置き、判断根拠を log.md に残す。すべての振り分け・移動を log.md に記録する（raw/ は git 管理外のため log が唯一の履歴）。frontmatter の `provided_by` は保存先の棚から推定して付与し、`share_scope` は明示がなければ `consent-required`。**インテーク時に使い手へ尋ねるのは、どうしても必要な場合でも1点まで。**
2. **`wiki/`への書き込み前に必ず使い手の承認を得る。** 承認なしの書き込みは禁止。
3. **個人情報の取り扱いはschema.md §3のSensitivity Levelに従う。** 実名・住所・電話番号等は`raw/`に留め、`wiki/`では匿名化IDで参照する。
4. **5ページ以上の同時変更は事前に一覧提示。** 一括書き込み禁止。
5. **矛盾するソースは両論併記 + `> [!warning]`コールアウト。** 勝手に正誤判断しない。本人の状態は変化するため、過去の記録と現在の記録の矛盾は「変化の証拠」として保存する。
6. **大量更新時はバックアップ。** 影響範囲を`wiki.backup_YYYYMMDD/`にコピーしてから実行。
7. **削除は禁止。** 不要ページは`status: stale`に変更し、移動を提案する。失敗の記録こそ将来の財産。
8. **本人の尊厳を損なう表現の禁止。** 「問題行動」「困った子」「手のかかる」等の評価的表現は使わない。代わりに「◯◯のときに××する傾向がある」と事実ベースで記述する。
9. **性・触法に関する記述はschema.md §3のプロトコルに従う。** 興味本位の詳細記述は禁止。「本人を守るため」「本人が他者を尊重できるよう支援するため」の情報のみ記録する。
10. **Neo4j支援DBとの整合性。** Person IDが食い違う変更は事前に使い手に報告する。
11. **本人の自己決定権を他者の決定権を侵す根拠にしてはならない。** 「本人がそうしたいと言っているから」は、相手が嫌がっている行為（身体接触・追跡・関係の強要等）を正当化しない。purpose.md A-6を直接反映する。
12. **本人を「加害リスクのあるケース」として分類してはならない。** リスクスコアリング・脅威としての記述は禁止。記述は常に本人の成長と他者との関係構築を支える方向を向く。purpose.md A-9を直接反映する。

---

## §3 4つの動作モード

### 3-1 ingest（取り込み）— 2段階Chain-of-Thought

受付箱・会話・添付・`raw/` のソースを Wiki に統合する。

**Step 0: 振り分け宣言（インテーク）**

**まず受付箱（§1）を見る。**受付箱にファイルがあれば、あるいは会話や添付でソースが到着したら（後者は下記の規則で先に受付箱に原本を起こす）、保存先を**宣言して**（「事業所からの支援記録として `raw/30_事業所から/GH○○/` に保存します」）raw/ へ保存する。訂正があれば従い、なければそのまま進む（§2-1 黙認方式）。棚から `provided_by` を推定し、`share_scope` は明示がなければ `consent-required`。振り分けを log.md に1行記録。受付箱から取り込む場合はコピーではなく**移動**する（原本を1つに保ち、受付箱を空に戻すため）。宣言には**「原本の日付」を1行含める**（面談日・記録日・発行日など原本自身が持つ日付）。原本に日付がなければ `原本の日付: 不明` と書き、受付日で代用しない——発生日（事実時間）と記録日（`created`・知得時間）を混同しないため（schema-common.md §C）。

**チャットに貼られた語り・口述の扱い。**使い手がファイルを作らず、チャットに語りを貼った・口頭で述べた・添付で渡した場合は、AI がまず**受付箱に原本ファイルを起こす**（`YYYY-MM-DD_出所_要旨.md`。先頭に「原本の日付」「出所（本人・家族・事業所・相談支援など）」「受け取った経路（チャット貼り付け／口述）」を記す。本文は言い換えずそのまま写す）。そのうえで、受付箱に置かれたファイルと**同じ仕分け経路**（宣言→raw/ へ移動→wiki/ に編む）に乗せる。チャットの文面を raw/ へ直接書き込んだり、原本を起こさずに wiki/ だけ作ったりしない——原本を1つ持つ原則（§2-1）を、入口が会話であっても崩さないため。

**Step 1: Analysis（分析）**

1. ソースを精読し、`purpose.md`をコンテキストとして読み込む（特に§3-0双方向性原則を確認）
2. 既存`index.md`と突き合わせて以下を抽出：
   - 言及されている人物（本人・家族・支援者・関わる他者）
   - 試行された対応とその結果（成功・失敗の両方）
   - 言及された概念・引き金・プロトコル
   - **本人が他者の決定権を学ぶ過程に関する記述**（特に対人関係・性に関する場面）
   - 既存ページとの接続点
   - 既存ページとの矛盾・緊張関係
   - Sensitivity Level判定（schema.md §3）
3. 統合計画（作成/更新/矛盾の一覧、Sensitivity Level別の分類）を使い手に提示。**本人の尊厳を損なう表現（§2-8）をソースや既存ページに見つけたら、使い手が指摘する前に AI から提起する**
4. **「Obsidianに保存してよいですか？」で明示的承認を得る**

**Step 2: Generation（生成）**

5. 承認後、`templates/`を元にページ生成・既存ページ追記（schema.md §2・schema-common.md §A）
6. 生成時にcross-reference（`[[wikilink]]`）を埋め込む
7. `index.md`、`log.md`、`overview.md`を更新
8. 人間判断が必要な項目は`wiki/reviews/`に積む（docs/review-system.md）
9. 完了サマリー（作成N件・更新N件・矛盾フラグN件・レビュー項目N件）を報告

### 3-2 query（照会）

Wiki内を横断検索して質問に回答する。

1. `purpose.md`と`index.md`を読み、関連ページを特定
2. 関連ページを精読して回答を合成（`[[...]]`で引用）
3. **回答が価値ある場合**、「この回答を`wiki/queries/`に保存しますか？」と確認
4. 保存時は`templates/query.md`を雛形に使用し、後のingestで他ページから参照可能にする

**回答するときの約束（時点の2軸。schema-common.md §C-4）**

- `valid_until` が記入された事実を「現在の事実」として答えない。答えるときは「〜まで有効だった」と期間を添える
- 鮮度切れ（staleAfter 超過・`valid_until` 空）のページから答えるときは、最終確認日（`last_confirmed`）を添える
- `valid_from` / `valid_until` は相談支援専門員が書く。AI は提案も推定もしない。`contradicts` で指されたページを見つけたら「いつまで当てはまっていましたか」と尋ねるところまで

### 3-3 lint（健全性チェック）

**必ず最初に `scripts/okf_lint.py` を実行する。** 目視や記憶による点検から始めてはならない。

```bash
cd <Vault のディレクトリ> && python3 scripts/okf_lint.py
```

依存パッケージなし。終了コードは `0`=違反なし / `1`=WARNのみ / `2`=ERROR（機微情報の漏出リスク・構造矛盾）。`--gate` は機微ゲートのみ、`--allowlist` は外部配布可能ファイルの一覧。

**スクリプトが機械的に検証する項目の一覧は scripts/README.md「検査項目一覧」が正典。**原理は schema-common.md §B・§C、本 Vault での型の割り当ては schema.md §6。

**鮮度 WARN の扱い**：鮮度は WARN であって ERROR ではない。`--gate` は ERROR の有無だけを終了コードに返すため、**鮮度切れで pre-commit・起動時ゲートは止まらない**。[鮮度] WARN を見つけたら使い手に平易に伝え（「P_001 の入浴プロトコル、最終確認が4ヶ月前です。今も機能していますか？」）、**使い手が確かめたと答えた場合にのみ** `last_confirmed` と `confirmed_by` を更新する。尋ねずに勝手に更新してはならない——確認していないものを確認済みにするのは、この仕組み全体への裏切りである。Trial を記録した際は、既存 protocol・trigger への `confirms` / `contradicts` を使い手に尋ねて記入し、success で protocol 通りなら指し先の `last_confirmed` を試行日に更新することを提案する（schema.md §2-2・§6-3）。**monitoring を記録した際は必ず**「今回のモニタリングで確認できた記録はどれですか」と尋ね、`confirms` に挙がったページの `last_confirmed` を `monitored_on` に更新することを提案する——モニタリングは鮮度更新の定期便である。

**外部配布ゲート**：`--allowlist` が返すのは `sensitivity: public` かつ `status: active|review` のページのみ。ERROR が1件でも出たページは無条件で除外される（fail-closed）。外部の検索基盤への投入・法人サイトへの転載・職員への配布は、**この一覧の範囲を超えてはならない**。

**スクリプトが検証しない項目**（AI が読解で点検する）：孤立ページ／未解消の矛盾フラグ／`status: stale` の放置／疎なコミュニティとブリッジノード（docs/graph-insight.md）／Trial の分布偏り／`decision-rights-learning` の Trial が半年以上無い（purpose.md G-8）／加害リスク分類的記述の混入（A-9）。`2` を返した場合は読解に進む前にまず ERROR を解消する。

### 3-4 graph-insight（グラフ洞察）— 発展機能

リンク構造から、事前定義のカテゴリとは独立に立ち現れる支援パターンを発見する。4シグナル関連性モデルと手順は docs/graph-insight.md。

---

## §4 Neo4j 支援DBとの連携

支援DB（Neo4j）が構造化情報と関係性のグラフクエリを担い、本 Vault が「行政書類には書けない人格と日常」を担う。**単独運用が既定**で、連携先がある環境でのみ有効化する（§6）。詳細は docs/連携.md。

- Person ID（`P_001` 形式）は両システムで共有する。食い違う変更は事前に使い手に報告する（§2-10）
- 重大な変更（GH 移行・診断変更等）は両方を更新する。先に支援DB を更新し、Wiki でナラティブを補完する順序
- **時点の2軸の対訳**（schema-common.md §C。Wiki は snake_case、支援DB は camelCase）:

| Wiki | 支援DB | 意味 |
|---|---|---|
| `created` | `registeredAt` | 記録日（不変） |
| `last_confirmed` | `lastConfirmedAt` | 最終確認日 |
| `valid_from` | `validFrom` | 事実の始点（任意） |
| `valid_until` | `validTo` | 事実の終点（裁定時に記入） |
| `superseded_by` / `valid_until_reason` | 終了日＋新規追加、裁定理由 | 置き換えの記録 |
| `confirms` / `contradicts` | `CONFIRMS` / `CONTRADICTS` | 証拠の連鎖（知得軸） |

---

## §5 起動時の挙動

新しいセッションでこのVaultに入った AI は、以下を順に実行する：

1. 本 AGENTS.md を読み込む
2. `purpose.md`を読む（特に§3-0双方向性原則を確認）
3. `index.md`の冒頭を確認（存在すれば）
4. `log.md`の最新3エントリを確認（存在すれば）
5. **`python3 scripts/okf_lint.py --gate` を実行する（必須）**
   - 終了コード `2` なら、作業に入る前に ERROR を使い手に報告する。機微情報の漏出リスクが未解消のまま ingest や query に進むことはしない
   - `--gate` が見るのは ERROR（機微・構造矛盾）のみ。鮮度の WARN はここでは止めない——全チェック（lint モード）で拾い、§3-3「鮮度 WARN の扱い」に従って使い手に確認を促す
   - 本Vaultは git 管理下にあり、`git config core.hooksPath githooks` で pre-commit 関所も有効化できる。ただし日常運用は commit を伴わないことが多いため、この起動時チェックを省略してはならない
   - Python3 が使えない環境では §0-4 のとおり wiki/ への書き込みを行わない
6. **読むときの約束を守る**（schema-common.md §C-4・§3-2）: `valid_until` のある事実を現在の事実として答えない。鮮度切れのページから答えるときは最終確認日を添える。`valid_until` を推定して書かない
7. 使い手に「今日は何モードで作業しますか？（ingest / query / lint / graph-insight / セットアップ）」を尋ねる

**必要なときに読む文書**（起動時には読まない。該当する場面で開く）:

| 場面 | 読む文書 |
|---|---|
| ページを新規作成・編集する | schema-common.md §A、schema.md §2・§4・§5 |
| sensitive 以上を扱う | schema.md §3、purpose.md §3-0 |
| 鮮度 WARN・時点の2軸に対応する | schema-common.md §B・§C、schema.md §6 |
| lint の検査項目を確認する | scripts/README.md |
| レビュー項目を起票する | docs/review-system.md |
| 支援DB（Neo4j）と連携する | docs/連携.md |
| 制度ウォッチ | docs/watchlist.md |

---

## §6 環境依存の付録（本則は AI 中立）

本則はどの AI エージェントでも実行できる書き方とし、環境固有の事項は以下に限る（導入時に docs/導入手順.md Step 3 で確認）。

- **ファイル操作の作法**: Vault のファイル操作は各エージェントの標準のファイル操作機能で行う。サンドボックス内のツールは Vault のファイルに届かないことがある。**部分編集を第一選択**にし、重要ファイル（本ファイル・purpose.md・schema.md・index.md）は差分を確認してから適用する。読み取りも必要な部分だけ
- **バイナリ（docx / pdf / xlsx / 画像）**: テキスト専用のファイルツールでは読めない。wiki/ に編む場合は使い手にテキスト抽出済みファイルを並置してもらう
- **支援DB 連携**: 連携先（Neo4j）がある環境でのみ §4 を有効化する。起動・接続の手順は本ファイルに書かない
- **スキル**: 手順の正典は常に本ファイル。スキルは該当節へ誘導するだけに留め、手順をスキル側に書かない

---

*本ファイルも生きた文書です。気づいた点は更新し、版の変更は log.md に記録します。*

*作成日: 2026-04-28 ／ 最終更新: 2026-09-05（減量・AI 中立化。旧 CLAUDE.md を改稿） ／ 管理者: 作者*
